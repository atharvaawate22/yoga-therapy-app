[pytest]
testpaths = tests
# `tests` is on sys.path so test modules can import the shared stubs from
# conftest directly; `.` makes `yoga_pose_engine` and `utils` importable the
# same way uvicorn sees them.
pythonpath = . tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
